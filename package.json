{
  "name": "hardhat-project",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.0",
    "@openzeppelin/contracts": "^4.8.1",
    "hardhat": "^2.12.6"
  },
  "scripts": { 
    "build": "npx hardhat compile && npx hardhat typechain && npx hardhat docgen"
  }
}
